# CUTLASS

本目录预留给 NVIDIA CUTLASS 学习与实验。CUTLASS 通过 C++ 模板提供高性能 GEMM、卷积及相关 building blocks，适合用作手写 CUDA kernel 的正确性/性能参照。

建议后续加入：依赖版本与获取方式、目标 GPU 架构、最小 GEMM 示例、编译命令，以及与 `operator/GEMM/` 相同输入尺寸下的 benchmark。

当前目录尚无 CUTLASS 源码或构建配置。
